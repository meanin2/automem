# Copy this file to ~/.config/automem/.env or export the values manually.
FALKORDB_HOST=localhost
FALKORDB_PORT=6379
FALKORDB_GRAPH=memories
QDRANT_URL=
QDRANT_API_KEY=
QDRANT_COLLECTION=memories
VECTOR_SIZE=768
PORT=8001
AUTOMEM_API_TOKEN=
ADMIN_API_TOKEN=

# --- Embedding Provider Configuration ---
# EMBEDDING_PROVIDER: auto (default), openai, gemini, local, placeholder
# - auto: tries OpenAI -> Gemini -> FastEmbed (local) -> Placeholder
# - openai: requires OPENAI_API_KEY
# - gemini: requires GEMINI_API_KEY (free tier available)
# - local: uses FastEmbed (no API key, runs locally)
# - placeholder: hash-based, no semantic meaning
EMBEDDING_PROVIDER=auto

# OpenAI (if using openai provider)
OPENAI_API_KEY=

# Google Gemini (if using gemini provider - free tier available!)
GEMINI_API_KEY=
# Task type for Gemini embeddings (optional, default: RETRIEVAL_DOCUMENT)
# Options: SEMANTIC_SIMILARITY, CLASSIFICATION, CLUSTERING, RETRIEVAL_DOCUMENT,
#          RETRIEVAL_QUERY, CODE_RETRIEVAL_QUERY, QUESTION_ANSWERING, FACT_VERIFICATION
GEMINI_TASK_TYPE=RETRIEVAL_DOCUMENT

# --- Testing / CI (optional) ---
# Enable integration test suite (defaults to disabled)
# AUTOMEM_RUN_INTEGRATION_TESTS=1
# Start/stop Docker Compose automatically for integration tests
# AUTOMEM_START_DOCKER=1
# AUTOMEM_STOP_DOCKER=1
# Override API base URL for integration tests (default http://localhost:8001)
# AUTOMEM_TEST_BASE_URL=http://localhost:8001
# Allow tests to run against a non-local host (requires explicit opt-in)
# AUTOMEM_ALLOW_LIVE=0
# Tokens the integration tests will use when calling the API
# AUTOMEM_TEST_API_TOKEN=
# AUTOMEM_TEST_ADMIN_TOKEN=
